$1 = Backbone.View.extend({

    defaults: {$2},

    events: {$3},

    tagName: '$4',
    className: '$5',


    initialize: function(options){
        this.options = _.extend({}, this.defaults, options);
        $6
    },

    render: function(){
       $7
    }
});